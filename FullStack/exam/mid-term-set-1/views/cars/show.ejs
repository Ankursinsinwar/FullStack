<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Car Details</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <div class="card mx-auto" style="width: 24rem;">
      <div class="card-body">
        <h5 class="card-title">Car Model: <%= car.carModel %></h5>
        <h6 class="card-subtitle mb-2 text-muted">Manufacturer: <%= car.manufacturer %></h6>
        <p class="card-text"><strong>VIN:</strong> <%= car.carVIN %></p>
        <p class="card-text"><strong>Year of Manufacture:</strong> <%= car.yearOfManufacture %></p>
        <p class="card-text"><strong>Mileage:</strong> <%= car.mileage %> km</p>
        <p class="card-text"><strong>Color:</strong> <%= car.color %></p>
        <p class="card-text"><strong>Price:</strong> $<%= car.price %></p>
        <p class="card-text"><strong>Electric:</strong> <%= car.isElectric ? "Yes" : "No" %></p>

        <a href="/cars/<%= car._id %>/edit" class="btn btn-warning">Edit</a>
        <form action="/cars/<%= car._id %>?_method=DELETE" method="POST" class="d-inline">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
        <a href="/cars" class="btn btn-secondary mt-2">Back to Cars</a>
      </div>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
